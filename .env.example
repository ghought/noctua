# Server-side (Railway)
ANTHROPIC_API_KEY=

# Client-side (Vite build) — baked into the native app bundle at build time
# Full URL of the Railway deployment (for native iOS/Android API calls)
VITE_API_URL=https://noctua.up.railway.app
# RevenueCat public SDK keys — get from RevenueCat dashboard → API Keys
VITE_REVENUECAT_IOS_KEY=
VITE_REVENUECAT_ANDROID_KEY=
