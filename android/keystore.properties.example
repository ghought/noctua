# android/keystore.properties  ← copy this file (remove .example), fill in values
# NEVER commit keystore.properties or the .jks file to git.

storeFile=keystore/noctua-release.jks
storePassword=YOUR_STORE_PASSWORD
keyAlias=noctua
keyPassword=YOUR_KEY_PASSWORD
